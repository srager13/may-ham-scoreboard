TODOs:
    - CURRENT:
    
    - GENERAL:
        - Fix websocket
        - Add real admin page
            - Add/register users? 
        - Auth - add actual passwords
          - have way to stay logged in

    - LEADERBOARD:
        - Make it more obvious when a team has won
        - Implement individual statistics
        - Show live matches

    - SCORING:
        - Make scoring a separate page for each hole (?)
        - Check high-low scoring

    - GROUPS:

    - TOURNAMENT SETUP:
        - refactor:
            - set matches for a round and auto-populate the pairings with the same matches
        - set tee time to 10 minutes from previous pairing by default
        - update existing tournament should update current tournament, not create a new one
            (loses any matches/scores/results already entered)

    - GOLF COURSES:
        
    - PRODUCTIONIZE:
        - Remove passwords from repo and add gitignores   ***
        - Create a dev and prod database version          ***
        - Create dev and prod versions of the servers     ***
            - protect the api/server (firewall?)
        - Create a release process                        ***
        - Put load balancer in front of server?
        - Backup database to obj storage?
        - Better testing - end-to-end/integration testing?

    - UI:
        - Optimize for mobile format